// This DTO is optional - we can use the variantId from the route parameter
// But keeping it for consistency and potential future validation needs
export class RemoveCartItemDto {
  // Empty for now, but can be extended with validation if needed
}
